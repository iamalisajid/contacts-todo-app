import React from 'react'
import PropTypes from 'prop-types'
import ContactsRow from "./rowContact";

const ContactsList = (props) => {
    return (
        <div>
            <ContactsRow/>
        </div>
    );
}

export default ContactsList