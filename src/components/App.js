import React from 'react';
import Routes from '../routes';

const App = ({props}) => {
    return (
        <div className="jumbotron">
            <div className="container">
                <Routes />
            </div>
        </div>
    );
}

export default App;
