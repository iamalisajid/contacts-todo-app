import React from 'react'
import PropTypes from 'prop-types'

const Todos = (props) => {
    return (
        <div>
            <h1>Todos</h1>
        </div>
    );
}

//Todos.propTypes = {}

export default Todos